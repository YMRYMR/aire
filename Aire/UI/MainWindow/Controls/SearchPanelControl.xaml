<UserControl x:Class="Aire.UI.MainWindow.Controls.SearchPanelControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Border x:Name="PART_SearchPanel"
            Visibility="Collapsed"
            VerticalAlignment="Top"
            HorizontalAlignment="Right"
            Background="{DynamicResource SurfaceBrush}"
            BorderBrush="{DynamicResource BorderBrush}"
            BorderThickness="1 0 0 1"
            CornerRadius="0 0 0 8"
            Padding="8 6"
            Panel.ZIndex="10">
        <StackPanel Orientation="Horizontal">
            <TextBox x:Name="PART_SearchTextBox"
                     Width="180"
                     Height="32"
                     Padding="6 2"
                     Background="{DynamicResource Surface2Brush}"
                     Foreground="{DynamicResource TextBrush}"
                     CaretBrush="{DynamicResource TextBrush}"
                     SelectionBrush="{DynamicResource PrimaryHoverBrush}"
                     SelectionTextBrush="{DynamicResource TextBrush}"
                     VerticalContentAlignment="Center"
                     TextChanged="SearchTextBox_TextChanged"
                     KeyDown="SearchTextBox_KeyDown" />
            <TextBlock x:Name="PART_SearchCountText"
                       VerticalAlignment="Center"
                       Foreground="{DynamicResource TextSecondaryBrush}"
                       FontSize="11"
                       MinWidth="44"
                       TextAlignment="Center"
                       Margin="4 0" />
            <Button x:Name="PART_SearchPrevButton"
                    Content="&#x2227;"
                    ToolTip="Previous match"
                    Click="SearchPrevButton_Click"
                    Width="28"
                    Height="28"
                    Padding="0"
                    Margin="0 0 2 0" />
            <Button x:Name="PART_SearchNextButton"
                    Content="&#x2228;"
                    ToolTip="Next match"
                    Click="SearchNextButton_Click"
                    Width="28"
                    Height="28"
                    Padding="0"
                    Margin="0 0 4 0" />
            <Button x:Name="PART_CloseSearchButton"
                    Content="&#x00D7;"
                    ToolTip="Close"
                    Click="CloseSearchButton_Click"
                    Width="28"
                    Height="28"
                    Padding="0" />
        </StackPanel>
    </Border>
</UserControl>
