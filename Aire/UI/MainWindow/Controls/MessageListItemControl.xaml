<UserControl x:Class="Aire.UI.MainWindow.Controls.MessageListItemControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:ui="clr-namespace:Aire.UI">
    <UserControl.Resources>
        <ui:PercentageConverter x:Key="PercentageConverter" />
    </UserControl.Resources>
    <Grid Margin="0 3">
        <Border x:Name="DateSeparator"
                Visibility="Collapsed"
                Margin="0 8">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <Border Background="{DynamicResource Surface2Brush}"
                        CornerRadius="10"
                        Padding="14 4">
                    <TextBlock Text="{Binding Text}"
                               FontSize="{Binding FontSize, RelativeSource={RelativeSource AncestorType=UserControl}}"
                               Foreground="{DynamicResource TextSecondaryBrush}" />
                </Border>
            </StackPanel>
        </Border>
        <Border x:Name="OrchestratorNarrativeBorder"
                Visibility="Collapsed"
                HorizontalAlignment="Left"
                VerticalAlignment="Top"
                Margin="0 2"
                Padding="14 10"
                CornerRadius="14 14 14 4"
                Background="{DynamicResource OrchestratorMessageBrush}"
                BorderBrush="{DynamicResource OrchestratorMessageBorderBrush}"
                BorderThickness="1"
                MaxWidth="{Binding RelativeSource={RelativeSource AncestorType=ScrollViewer}, Path=ActualWidth, Converter={StaticResource PercentageConverter}, ConverterParameter=0.7}"
                Panel.ZIndex="2">
            <TextBox Text="{Binding Text, Mode=OneWay}"
                     Foreground="{DynamicResource OrchestratorMessageTextBrush}"
                     FontSize="{Binding FontSize, RelativeSource={RelativeSource AncestorType=UserControl}}"
                     TextWrapping="Wrap"
                     TextAlignment="Left"
                     HorizontalContentAlignment="Left"
                     VerticalContentAlignment="Top"
                     BorderThickness="0"
                     Background="Transparent"
                     Padding="0"
                     IsReadOnly="True"
                     IsReadOnlyCaretVisible="False"
                     IsTabStop="False"
                     Focusable="True"
                     Cursor="IBeam"
                     VerticalScrollBarVisibility="Disabled"
                     HorizontalScrollBarVisibility="Disabled"
                     FocusVisualStyle="{x:Null}" />
        </Border>
        <Border x:Name="DeniedActionBorder"
                Visibility="Collapsed"
                HorizontalAlignment="Left"
                VerticalAlignment="Top"
                Margin="0 2"
                Padding="14 10"
                CornerRadius="14 14 14 4"
                Background="{DynamicResource ErrorBrush}"
                BorderBrush="{DynamicResource ErrorBrush}"
                BorderThickness="1"
                MaxWidth="{Binding RelativeSource={RelativeSource AncestorType=ScrollViewer}, Path=ActualWidth, Converter={StaticResource PercentageConverter}, ConverterParameter=0.7}"
                Panel.ZIndex="2">
            <StackPanel Orientation="Vertical">
                <TextBlock Text="{Binding ToolCallStatus, Mode=OneWay}"
                           Foreground="{DynamicResource TextBrush}"
                           FontSize="{Binding FontSize, RelativeSource={RelativeSource AncestorType=UserControl}}"
                           FontWeight="SemiBold"
                           TextWrapping="Wrap"
                           Margin="0 0 0 2" />
                <TextBlock Text="{Binding DeniedToolCallActionText, Mode=OneWay}"
                           Foreground="{DynamicResource TextBrush}"
                           FontSize="{Binding FontSize, RelativeSource={RelativeSource AncestorType=UserControl}}"
                           TextWrapping="Wrap" />
            </StackPanel>
        </Border>
        <TextBox x:Name="SystemText"
                 Text="{Binding Text, Mode=OneWay}"
                 Foreground="{Binding SenderForeground}"
                 FontSize="{Binding FontSize, RelativeSource={RelativeSource AncestorType=UserControl}}"
                 HorizontalAlignment="Center"
                 TextWrapping="Wrap"
                 Margin="20 2"
                 Visibility="Collapsed"
                 IsReadOnly="True"
                 BorderThickness="0"
                 Background="Transparent"
                 Padding="0"
                 VerticalContentAlignment="Center"
                 HorizontalContentAlignment="Center"
                 Focusable="True"
                 Cursor="IBeam"
                 IsReadOnlyCaretVisible="False"
                 VerticalScrollBarVisibility="Disabled"
                 HorizontalScrollBarVisibility="Disabled" />
        <StackPanel x:Name="MessageRow"
                    Orientation="Horizontal"
                    HorizontalAlignment="Left">
            <Border x:Name="MessageBorder"
                    CornerRadius="14 14 14 4"
                    Padding="12 8"
                    Background="{DynamicResource AssistantMessageBrush}"
                    HorizontalAlignment="Left"
                    MaxWidth="{Binding RelativeSource={RelativeSource AncestorType=ScrollViewer}, Path=ActualWidth, Converter={StaticResource PercentageConverter}, ConverterParameter=0.7}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <ItemsControl x:Name="InlineImagesPanel"
                                  Grid.Row="0"
                                  ItemsSource="{Binding InlineImages}"
                                  Visibility="Collapsed"
                                  Margin="0 0 0 6">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Image Source="{Binding}"
                                       MaxWidth="280"
                                       MaxHeight="220"
                                       Stretch="Uniform"
                                       Margin="0 0 0 6"
                                       HorizontalAlignment="Left"
                                       Cursor="Hand"
                                       ToolTip="Click to open in image viewer"
                                       MouseLeftButtonUp="InlineImage_MouseLeftButtonUp" />
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                    <Image x:Name="AttachedImageElement"
                           Grid.Row="1"
                           Source="{Binding AttachedImage, Mode=OneWay}"
                           Visibility="Collapsed"
                           MaxWidth="280"
                           MaxHeight="200"
                           Stretch="Uniform"
                           Margin="0 0 0 6"
                           HorizontalAlignment="Left"
                           Cursor="Hand"
                           ToolTip="Click to open in image viewer"
                           MouseLeftButtonUp="AttachedImageElement_MouseLeftButtonUp" />
                    <ItemsControl x:Name="FileAttachmentsPanel"
                                  Grid.Row="2"
                                  ItemsSource="{Binding FileAttachments}"
                                  Visibility="Collapsed"
                                  Margin="0 0 0 6">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Button Content="{Binding DisplayLabel}"
                                        Margin="0 0 0 6"
                                        Padding="10 4"
                                        HorizontalAlignment="Left"
                                        Focusable="True"
                                        AutomationProperties.Name="{Binding DisplayLabel}"
                                        AutomationProperties.HelpText="Open attached file"
                                        ToolTip="{Binding FilePath}"
                                        Click="FileAttachmentButton_Click" />
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                    <RichTextBox x:Name="MessageText"
                                 Grid.Row="3"
                                 ui:LinkedText.Text="{Binding Text}"
                                 Foreground="{DynamicResource AssistantMessageTextBrush}"
                                 IsReadOnly="True"
                                 IsReadOnlyCaretVisible="False"
                                 IsDocumentEnabled="True"
                                 BorderThickness="0"
                                 Background="Transparent"
                                 Padding="0"
                                 MinWidth="0"
                                 SelectionBrush="{DynamicResource PrimaryHoverBrush}"
                                 VerticalScrollBarVisibility="Disabled"
                                 HorizontalScrollBarVisibility="Disabled"
                                 IsTabStop="False"
                                 HorizontalAlignment="Left"
                                 Cursor="IBeam"
                                 FocusVisualStyle="{x:Null}" />
                    <TextBox x:Name="PlainMessageText"
                             Grid.Row="3"
                             Text="{Binding Text, Mode=OneWay}"
                             Foreground="{DynamicResource AssistantMessageTextBrush}"
                             FontSize="{Binding FontSize, RelativeSource={RelativeSource AncestorType=UserControl}}"
                             TextWrapping="Wrap"
                             Visibility="Collapsed"
                             HorizontalAlignment="Left"
                             VerticalAlignment="Top"
                             HorizontalContentAlignment="Left"
                             VerticalContentAlignment="Top"
                             Cursor="IBeam"
                             Margin="0"
                             Padding="0"
                             MinWidth="0"
                             BorderThickness="0"
                             Background="Transparent"
                             IsReadOnly="True"
                             IsReadOnlyCaretVisible="False"
                             IsTabStop="False"
                             Focusable="True"
                             VerticalScrollBarVisibility="Disabled"
                             HorizontalScrollBarVisibility="Disabled"
                             FocusVisualStyle="{x:Null}" />
                    <RichTextBox x:Name="ToolCallDescriptionText"
                                 Grid.Row="4"
                                 ui:LinkedText.Text="{Binding PendingToolCall.Description, Mode=OneWay}"
                                 Visibility="Collapsed"
                                 Foreground="{DynamicResource TextBrush}"
                                 FontSize="{Binding FontSize, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                 Margin="0 4 0 0"
                                 MinWidth="0"
                                 IsReadOnly="True"
                                 IsReadOnlyCaretVisible="False"
                                 IsDocumentEnabled="True"
                                 BorderThickness="0"
                                 Background="Transparent"
                                 Padding="0"
                                 VerticalContentAlignment="Top"
                                 HorizontalAlignment="Left"
                                 Focusable="True"
                                 Cursor="IBeam"
                                 VerticalScrollBarVisibility="Hidden"
                                 HorizontalScrollBarVisibility="Disabled"
                                 FocusVisualStyle="{x:Null}" />
                    <RichTextBox x:Name="ToolCallStatusText"
                             Grid.Row="5"
                             ui:LinkedText.Text="{Binding ToolCallStatus, Mode=OneWay}"
                             Visibility="Collapsed"
                             Foreground="{DynamicResource TextSecondaryBrush}"
                             FontSize="{Binding FontSize, RelativeSource={RelativeSource AncestorType=UserControl}}"
                             Margin="0 4 0 0"
                             MinWidth="0"
                             IsReadOnly="True"
                             BorderThickness="0"
                             Background="Transparent"
                             Padding="0"
                             VerticalContentAlignment="Top"
                             HorizontalAlignment="Left"
                             Focusable="True"
                             Cursor="IBeam"
                             IsReadOnlyCaretVisible="False"
                             VerticalScrollBarVisibility="Hidden"
                             HorizontalScrollBarVisibility="Disabled"
                             IsDocumentEnabled="True"
                             IsTabStop="False"
                             FocusVisualStyle="{x:Null}" />
                    <Border x:Name="TodoListPanel"
                            Grid.Row="6"
                            Visibility="Collapsed"
                            Margin="0 6 0 0">
                        <ItemsControl ItemsSource="{Binding TodoItems}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Grid Margin="0 3">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="22" />
                                            <ColumnDefinition Width="*" />
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Grid.Column="0"
                                                   Text="{Binding StatusIcon}"
                                                   Foreground="{Binding StatusColor}"
                                                   FontWeight="SemiBold"
                                                   FontSize="{Binding FontSize, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                   VerticalAlignment="Top"
                                                   Margin="0 1 0 0" />
                                        <TextBlock Grid.Column="1"
                                                   Text="{Binding Description}"
                                                   TextWrapping="Wrap"
                                                   FontSize="{Binding FontSize, RelativeSource={RelativeSource AncestorType=UserControl}}">
                                            <TextBlock.Style>
                                                <Style TargetType="TextBlock">
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding IsCompleted}" Value="True">
                                                            <Setter Property="TextDecorations" Value="Strikethrough" />
                                                            <Setter Property="Opacity" Value="0.55" />
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBlock.Style>
                                        </TextBlock>
                                    </Grid>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </Border>
                    <Border x:Name="FollowUpPanel"
                            Grid.Row="7"
                            Visibility="Collapsed"
                            Margin="0 6 0 0">
                        <StackPanel>
                            <TextBlock Text="{Binding FollowUpQuestion}"
                                       TextWrapping="Wrap"
                                       FontWeight="SemiBold"
                                       FontSize="{Binding FontSize, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                       HorizontalAlignment="Left"
                                       Margin="0 0 0 8" />
                            <ItemsControl x:Name="AnswerButtonsPanel"
                                          ItemsSource="{Binding FollowUpOptions}"
                                          Visibility="Collapsed">
                                <ItemsControl.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <WrapPanel />
                                    </ItemsPanelTemplate>
                                </ItemsControl.ItemsPanel>
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Button Content="{Binding}"
                                                Margin="0 0 6 6"
                                                Click="AnswerButton_Click" />
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </StackPanel>
                    </Border>
                    <Image x:Name="ScreenshotImageElement"
                           Grid.Row="8"
                           Source="{Binding ScreenshotImage, Mode=OneWay}"
                           Visibility="Collapsed"
                           MaxWidth="400"
                           MaxHeight="300"
                           Stretch="Uniform"
                           Margin="0 6 0 0"
                           HorizontalAlignment="Left"
                           Cursor="Hand"
                           ToolTip="Click to open in image viewer"
                           MouseLeftButtonUp="ScreenshotImageElement_MouseLeftButtonUp" />
                    <TextBlock Grid.Row="9"
                               Text="{Binding Timestamp}"
                               FontSize="{Binding FontSize, RelativeSource={RelativeSource AncestorType=UserControl}}"
                               Foreground="{DynamicResource TextSecondaryBrush}"
                               HorizontalAlignment="Right"
                               Margin="0 3 0 0" />
                </Grid>
            </Border>
            <StackPanel x:Name="ToolApprovalPanel"
                        Visibility="Collapsed"
                        VerticalAlignment="Center"
                        Margin="6 0 0 0">
                <Button x:Name="ApproveToolButton"
                        Content="&#x2713;"
                        ToolTip="Allow"
                        Tag="{Binding}"
                        Click="ApproveToolButton_Click"
                        Width="34"
                        Height="34"
                        Margin="0 0 0 4" />
                <Button x:Name="DenyToolButton"
                        Content="&#x2717;"
                        ToolTip="Deny"
                        Tag="{Binding}"
                        Click="DenyToolButton_Click"
                        Width="34"
                        Height="34" />
            </StackPanel>
        </StackPanel>
    </Grid>
</UserControl>
