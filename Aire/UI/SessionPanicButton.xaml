<Window x:Class="Aire.UI.SessionPanicButton"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Aire Stop"
        Width="130"
        Height="52"
        WindowStyle="None"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        AllowsTransparency="True"
        Background="Transparent">

    <!-- Draggable container so the user can reposition the button -->
    <Border x:Name="DragBorder"
            Background="#E5220808"
            BorderBrush="#FF550000"
            BorderThickness="1"
            CornerRadius="7"
            MouseDown="DragBorder_MouseDown"
            Cursor="SizeAll"
            ToolTip="Click to stop the active keyboard/mouse session immediately.&#x0a;Drag to reposition.">
        <Button x:Name="StopButton"
                Content="&#x23F9; STOP SESSION"
                FontSize="12"
                FontWeight="Bold"
                Foreground="White"
                Background="Transparent"
                BorderThickness="0"
                Cursor="Hand"
                Click="StopButton_Click" />
    </Border>
</Window>
