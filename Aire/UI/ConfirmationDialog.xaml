<Window x:Class="Aire.UI.ConfirmationDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Confirm"
        SizeToContent="Height"
        Width="320"
        WindowStyle="None"
        ResizeMode="NoResize"
        ShowInTaskbar="False">
    <WindowChrome.WindowChrome>
        <WindowChrome CaptionHeight="0" GlassFrameThickness="0" ResizeBorderThickness="0" UseAeroCaptionButtons="False" />
    </WindowChrome.WindowChrome>

    <Border Background="{DynamicResource BackgroundBrush}"
            BorderBrush="{DynamicResource AccentBorderBrush}"
            BorderThickness="1">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <!-- Title bar -->
            <Border Grid.Row="0"
                    Background="{DynamicResource AccentSurfaceBrush}"
                    BorderBrush="{DynamicResource AccentBorderBrush}"
                    BorderThickness="0 0 0 1"
                    Padding="14 10"
                    MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                <TextBlock x:Name="TitleText"
                           Text="Clear all messages?"
                           Foreground="{DynamicResource TextBrush}"
                           FontWeight="SemiBold" />
            </Border>

            <!-- Content -->
            <StackPanel Grid.Row="1" Margin="14 14 14 14">
                <TextBlock x:Name="MessageText"
                           Text="This will permanently delete the conversation from the database."
                           TextWrapping="Wrap"
                           Foreground="{DynamicResource TextSecondaryBrush}"
                           Margin="0 0 0 16" />

                <StackPanel Orientation="Horizontal"
                            HorizontalAlignment="Right">
                    <Button x:Name="YesButton"
                            Content="Yes"
                            Width="70"
                            Margin="0 0 8 0"
                            Click="YesButton_Click" />
                    <Button x:Name="NoButton"
                            Content="No"
                            Width="70"
                            Click="NoButton_Click" />
                </StackPanel>
            </StackPanel>
        </Grid>
    </Border>
</Window>
